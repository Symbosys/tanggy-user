npx @react-native-community/cli@latest init MintafreshUser --package-name com.mintafresh.user


ref={bottomSheetRef}
snapPoints={['25%', '50%', '90%']}
initialSnapIndex={1}
enablePanDownToClose={true}
backdropOpacity={0.6}
onClose={() => console.log('Sheet closed')}



rm -rf node_modules
rm -rf android/app/build
rm -rf android/build
rm -rf android/.cxx
rm -rf android/app/.cxx
rm -rf android/app/build/generated
rm -rf android/.gradle281103





this is html  code i want you to convert this code into react native cli 



the ui should be exactly same as the html code contains 



the ui in react native should be as same as the html code Ui contains 



inch by inch margin by margin padding my padding in every aspect the Ui should be exactly same as the html code contains 

use fontweight value eighter 800 or more than 800 don't use below 800 or use bold or extrabold  fontweight: 800  

use this theme color 

export const COLORS = {
  primary: '#9235D0',    // Deep Purple (for buttons, highlights)
  secondary: '#f9eae9',  // Soft Pink (for backgrounds, cards)
  accent: '#b58ff0',     // Lavender (hover, subtle accents)
  highlight: '#ff9fa3',  // Coral/Peach (alerts, badges)
  background: '#fafafa', // Default background
  white: '#ffffff',       // White color
  textPrimary: '#222222', // Main text
  textSecondary: '#4a4a4a', // Secondary text
  muted: '#888888',       // Muted text or icons
};


use this color which is located in ../../theme/theme and if you need beyond this theme color and hardcode them  wrap entire code with safeareview import from safearea context